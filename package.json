{
    "files": [
        "src/"
    ],
    "scripts": {
        "start": "parcel serve src/index.html",
        "build": "call remove-dist && cross-env NODE_ENV=production parcel build src/index.html --public-url ."
    },
    "devDependencies": {
        "cross-env": "^5.2.0",
        "parcel-bundler": "^1.9.7",
        "typescript": "^2.9.2"
    }
}