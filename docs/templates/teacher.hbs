<li id="teacher-{{ID}}">
    <p>{{Name}}</p>
    <div class="teacher-ofo-assignments">
        {{#each OfoAssignment}}
            <div onclick="OpenTabloModal({{TabloID}})" title="Ekkor volt a(z) {{Class.Grade}}.{{Class.Sub}} osztály osztályfőnöke">
                {{From}} - {{To}}
            </div>
        {{/each}}
    </div>
</li>